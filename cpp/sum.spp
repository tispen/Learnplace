#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int sum=0,now;
    cin >> now;
    while (now!=0) {
        sum += now;
        cin >> now;
    }
    cout << sum;
    return 0;
}
